/** Number of days after an event that PPV purchases can still be made. */
export const PURCHASE_WINDOW_DAYS = 2;

/** Number of days after an event that replays remain available. */
export const REPLAY_WINDOW_DAYS = 4;

/** Number of hours of access granted from the moment a VOD is purchased. */
export const VOD_ACCESS_HOURS = 72;
